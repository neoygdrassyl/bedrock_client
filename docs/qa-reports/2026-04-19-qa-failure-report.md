## QA Failure Report — 2026-04-19

### Summary
- P0 (Critical): 0 findings
- P1 (Functional): 5 findings
- P2 (Contract): 5 findings
- P3 (Visual): 0 findings
- P4 (Debt): 0 findings

Smoke coverage stayed green (`npm test`: 71 files / 464 tests, `playwright --grep-invert=intermediate`: 31 passed / 4 skipped), but targeted static review + Playwright probes still uncovered redesign regressions and contract gaps that those suites do not currently cover.

### Findings

| # | Priority | Category | File(s) | Description | Reproduction | Suggested Fix |
|---|----------|----------|---------|-------------|--------------|---------------|
| 1 | P1 | Functional Regression | `src/app/services/data.service.js`, `src/app/App.js`, `src/app/layouts/AppShell.jsx`, `src/app/layouts/navigation-config.js` | `auth.user` loses `role_short`, `roleDesc`, `name_full` and other session fields because `getUserData()` strips them. `AppShell` then calls `getNavItems(undefined)` and renders every rail item, including admin-only modules, for non-admin sessions. | Runtime probe: inject `dovela_user` with `role_short: 'USER'`, open `/dashboard`, and the shell shows 10 rail items (`Licencias`, `Mensajes`, `Archivo`, etc.) instead of the USER subset. Code path: read `data.service.js:15-23`, `App.js:317-328`, `AppShell.jsx:22`, `navigation-config.js:110-114`. | Preserve the full login payload in auth state (or at minimum `role_short`, `roleDesc`, `name_full`) and add a regression test that checks rail visibility for USER/AUX/admin sessions. |
| 2 | P2 | Contract Violation | `src/app/layouts/navigation-config.js`, `src/app/pages/auth/LoginPage.jsx`, `src/__tests__/Login.smoke.test.js`, `e2e/fixtures/auth.fixture.js` | The role contract is internally inconsistent: navigation expects `ADMIN`, while login/test fixtures already use `ADM`. Even after fixing finding #1, an `ADM` session still resolves to the non-admin subset. | Read `navigation-config.js:6,27,47,55,71,79,87`, `LoginPage.jsx:55`, `src/__tests__/Login.smoke.test.js:152`, `e2e/fixtures/auth.fixture.js:24`. Helper probe: `getNavItems('ADM')` returns only `dashboard/peticiones/calendario/normas`. | Normalize role codes at the session boundary (`ADM` -> `ADMIN`) or align `navigation-config.js` with the canonical backend codes and update tests/fixtures to the same source of truth. |
| 3 | P1 | Functional Regression | `src/app/layouts/navigation-config.js`, `src/app/App.js`, `src/app/pages/user/dashboard.js` | Internal shell navigation points to routes rendered outside `PrivateLayout`. Authenticated users clicking `Normas urbanas`, `Uso de suelo`, or the dashboard card `Historial de Profesionales` are ejected out of `AppShell` with no rail/header/footer. | Playwright probes: from `/dashboard`, click rail button `Normas urbanas` -> URL `/normas`, `shellPresent=0`; click card `Historial de Profesionales` -> URL `/certificados`, `shellPresent=0`. Code: `navigation-config.js:94-99,150-155`, `App.js:159-178,186-242`, `dashboard.js:117-127`. | Either expose these routes inside `PrivateLayout` for authenticated navigation or remove them from internal shell/dashboard entry points and open them explicitly as public modules. |
| 4 | P1 | Functional Regression | `src/app/App.js`, `src/app/layouts/AppShell.jsx`, `src/app/layouts/navigation-config.js` | `/sellos` is a private route without matching shell metadata. `AppShell` falls back to the first rail item and marks **Panel de control** as active while the user is actually on **Sellos**. | Open `/sellos` with an authenticated session; the page heading is `Sellos` but the active rail button is `Panel de control`. Code: `App.js:235-238`, `AppShell.jsx:24-31`; `navigation-config.js` defines a redirect for `/seals` but no `sellos` item. | Add route metadata for `/sellos` (and any other non-rail private routes) or teach `AppShell` to represent “route outside rail” without defaulting to dashboard. |
| 5 | P2 | Contract Violation | `src/components/data-table-bridge.jsx`, `src/app/styles/legacy-bridge.css`, `src/__tests__/DataTableLegacyCompat.unit.test.jsx` | `DataTableBridge` does not emit `.rdt_TableHead` or `.rdt_TableCell`, even though the redesign relies on legacy `rdt_*` hooks for CSS compatibility. The current regression test only asserts `.rdt_Table` and `.rdt_TableRow`, so the missing selectors slipped through. | Runtime probe on `/licencias`: `.rdt_TableRow=1`, `.rdt_TableHead=0`, `.rdt_TableCell=0`. Code: `data-table-bridge.jsx:194-215,255-259`; compatibility CSS exists in `legacy-bridge.css:365-388,748-770`, but there is no class emission for head/cell nodes. | Add the missing class names to header/cell nodes and extend the compatibility test to assert the full selector contract (`rdt_TableHead`, `rdt_TableHeadRow`, `rdt_TableRow`, `rdt_TableCell`, `rdt_Pagination`). |
| 6 | P1 | Functional Regression | `src/components/data-table-bridge.jsx`, `src/app/pages/user/submit/submit.js`, `src/app/pages/user/pqrs/pqrsadmin.js`, `src/app/pages/user/fun.js` | `paginationRowsPerPageOptions` and `paginationComponentOptions` are accepted by the bridge but never used. After migrating away from `react-data-table-component`, rows-per-page controls and localized pagination copy disappeared across many tables. | Read `data-table-bridge.jsx:35-37` and search callers such as `submit.js:403-411`, `pqrsadmin.js:932-1010`, `fun.js:1474-1643`; dozens of migrated tables still pass these props, but the bridge never renders a selector or consumes the label options. | Implement rows-per-page selection and localized labels in the bridge, or deprecate those props explicitly and migrate all callers to a new supported API. |
| 7 | P1 | Functional Regression | `src/app/pages/user/dashboard.js` | Dashboard real-time counts fail silently. When count APIs reject, FUN cards are coerced to `0` while the other module counts go blank, and the UI shows no visible error or fallback state. This turns transport failure into misleading business data. | Playwright probe forcing all fetch/XHR calls to return `500`: dashboard still renders, first card text becomes `Radicar Licencias0…`, and visible error text count stays `0`. Code: `dashboard.js:67-97,198-204`. | Preserve error/null state per card, render a visible fallback badge/banner, and never coerce failed count requests to numeric zero. |
| 8 | P2 | Contract Violation | `src/app/pages/user/fun_forms/components/FunExpedienteDetail.jsx`, `src/app/pages/user/fun_forms/components/FunExpedienteWorkspace.jsx`, `src/app/pages/user/fun_forms/components/FunmanagePhaseChart.jsx`, `src/app/pages/user/fun_forms/components/FunmanageScatterChart.jsx`, `src/app/pages/user/fun_forms/components/FunDashboardKPIs.jsx` | The new FUN Manage redesign bypasses the token system with hardcoded `#fff`, hex palettes, rgba borders/shadows and inline color values. The newest module is therefore not theme-safe and will drift visually from the redesign system, especially in dark mode. | Read `FunExpedienteDetail.jsx:9-12,68-92,113-149`, `FunExpedienteWorkspace.jsx:36-58,282-330,350-352`, `FunmanagePhaseChart.jsx:15-35,65-77`, `FunmanageScatterChart.jsx:20-25,43-45,66-118`, `FunDashboardKPIs.jsx:11-12,21-22,178-189,242-246,278-290`. | Replace inline hex/rgba/white styles with semantic tokens/Tailwind classes and add a dark-mode smoke for `/licencias/gestion-nueva`. |
| 9 | P2 | Contract Violation | `src/components/ui/button.jsx`, `src/components/ui/dropdown-menu.jsx`, `src/components/ui/dialog.jsx`, `src/components/ui/input.jsx`, `src/components/ui/table.jsx`, `src/components/ui/*.jsx` | The redesigned UI layer introduces widespread `React.forwardRef()` usage in app-owned source files, violating the repo rule forbidding `forwardRef` in new code. | `grep -RIn "forwardRef" src/components/ui src/app/layouts src/app/pages/auth` returns the full shadcn wrapper layer (`button.jsx`, `dialog.jsx`, `dropdown-menu.jsx`, `table.jsx`, etc.). | Either codify an explicit exception for vendored shadcn wrappers or migrate app-owned wrappers to React 19-compatible ref-as-prop patterns so the repo rule matches reality. |
| 10 | P2 | Contract Violation | `src/app/pages/auth/LoginPage.jsx` | The redesigned login screen still hardcodes gradient stops with literal HSL values instead of semantic tokens, so one of the flagship redesign pages is not fully token-driven. | Read `LoginPage.jsx:133`. Static grep over redesign paths returns this login gradient as a hardcoded color hit in the new shell/auth code. | Move the gradient stops into semantic tokens/CSS variables so light/dark theming and future brand changes do not require component edits. |
