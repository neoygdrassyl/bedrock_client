Rule: react-doctor/prefer-use-sync-external-store
Severity: warning
Category: State & Effects
Count: 1

useState "recentExpedientes" is synchronized with an external store via useEffect — replace this useState + useEffect pair with useSyncExternalStore(subscribe, getSnapshot) to avoid tearing during concurrent renders

Suggestion: Replace the `useState(getSnapshot())` + `useEffect(() => store.subscribe(() => setSnapshot(getSnapshot())))` pair with `useSyncExternalStore(store.subscribe, getSnapshot)`. The hook handles tearing during concurrent renders and SSR snapshots; the manual subscribe pattern doesn't

Files:
  src/app/pages/user/dashboard.js:132
