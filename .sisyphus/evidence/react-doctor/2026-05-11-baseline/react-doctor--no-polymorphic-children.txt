Rule: react-doctor/no-polymorphic-children
Severity: warning
Category: Architecture
Count: 1

Polymorphic `typeof children === "string"` check — expose explicit subcomponents (e.g. `<Button.Text>`) instead of branching on what the consumer passed

Suggestion: Expose explicit subcomponents (`<Button.Text>`, `<Button.Icon>`) so consumers don't need to switch on `typeof children`

Files:
  src/app/components/MermaidDiagram.component.js:543
